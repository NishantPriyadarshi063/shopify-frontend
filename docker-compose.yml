# Frontend only. Run from shopify-frontend: docker compose up -d --build
# Browser calls backend directly (same host, port 5300). No proxy, no .env for API URL.

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    environment:
      PORT: 5299
    ports:
      - "5299:5299"
